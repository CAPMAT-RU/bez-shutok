```astro---
import { getCollection } from 'astro:content';

// Получаем все записи из нашей коллекции 'posts'
const allPosts = await getCollection('posts');
---



Мой блог


{allPosts.map((post) => (


{post.data.title}

{post.data.description}


))}



```
